# 생성 산출물 (inspect_zdf 등 실행 시 재생성 가능) — 리포에 넣지 않음
data/output/

# build_bin_frame가 자동 생성하는 bin frame 캐시 (bin_roi_*.json 티칭 결과로부터 재계산)
config/bin_frame*.json

# 캡처 원본 (.zdf 1장이 약 86MB). 리포 용량 때문에 커밋하지 않는다.
# 배치 방법은 README.md "데이터 준비" 참조.
data/input/
